<UserControl
    x:Class="LocalTalk.Shared.Views.Device"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LocalTalk.Shared.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <SymbolIcon Symbol="{Binding glyph}" Grid.Column="0"/>

        <StackPanel HorizontalAlignment="Stretch" Grid.Column="1">
            <TextBlock Text="{Binding alias}"/>
            <TextBlock Text="{Binding deviceModel}"/>
        </StackPanel>

        <ToggleButton HorizontalAlignment="Right" Grid.Column="2">
            <SymbolIcon Symbol="Favorite"/>
        </ToggleButton>
    </Grid>
</UserControl>
