<UserControl
    x:Class="Shared.Views.SendPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:shared="using:Shared"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Shared.Views"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Hidden"
                      VerticalScrollMode="Disabled">
            <StackPanel Orientation="Horizontal">
                <AppBarButton Icon="OpenFile" Label="File"/>
                <AppBarButton Icon="BrowsePhotos" Label="Media"/>
                <AppBarButton Icon="Folder" Label="Folder"/>
                <AppBarButton Icon="Edit" Label="Text"/>
                <AppBarButton Icon="AllApps" Label="App installer"/>
                <AppBarButton Icon="Contact" Label="Contact"/>
                <AppBarButton Icon="Redo" Label="Backup (Windows Phone only)"/>
            </StackPanel>
        </ScrollViewer>

        <ListView Name="PendingUploads" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:TransferItem/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ListView Name="TargetList" Grid.Row="2"
                  ItemsSource="{x:Bind shared:LocalSendProtocol.Devices}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:Device/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
